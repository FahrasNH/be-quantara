"use strict";

/** Default 5-coin allowlist for multi-symbol walk-forward promotion grids. */
const DEFAULT_SYMBOLS_5 = ["BTCUSDT", "ETHUSDT", "BNBUSDT", "XRPUSDT", "SOLUSDT"];

const DEFAULT_SYMBOL_BTC = ["BTCUSDT"];

module.exports = {
  DEFAULT_SYMBOLS_5,
  DEFAULT_SYMBOL_BTC,
};
